spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  cloud:
    aws:
      region:
        static: ${AWS_S3_REGION}

app:
  aws:
    s3:
      bucket: ${AWS_S3_BUCKET}
