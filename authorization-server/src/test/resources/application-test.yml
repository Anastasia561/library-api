testcontainers:
  mysql:
    image: mysql:8.0.33
    username: ${TEST_USER}
    password: ${TEST_PASSWORD}
    database: ${TEST_DB}

spring:
  sql:
    init:
      mode: always
      schema-locations: file:./db/01_schema.sql

app:
  security:
    allowed-origin: ${ALLOWED_ORIGIN}
