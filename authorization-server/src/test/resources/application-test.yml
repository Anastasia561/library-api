testcontainers:
  reuse.enable: true
  mysql:
    image: mysql:8.0.33
    username: ${TEST_USER}
    password: ${TEST_PASSWORD}
    database: ${TEST_DB}

spring:
  sql:
    init:
      mode: always

app:
  security:
    allowed-origin: ${ALLOWED_ORIGIN}
